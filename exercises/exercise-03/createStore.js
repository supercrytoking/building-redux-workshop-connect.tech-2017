export default (reducer) => {
    let state;
    const store = {
        dispatch: () => {

        },
        getState: () => {
            return state;
        },
        subscribe: () => {
        },
    };

    return store;
};
